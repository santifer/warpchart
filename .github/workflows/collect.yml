name: collect
on:
  schedule:
    - cron: "7 * * * *" # :07 avoids the :00 congestion on Actions
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: collect
  cancel-in-progress: false

jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
      - run: node collector/collect.mjs
        env:
          # The automatic Actions token covers public-data reads (1,000 req/h,
          # the collector uses ~12). No PAT or secret setup needed.
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Optional: a Discord or Slack incoming-webhook URL. If set, the
          # collector posts milestone gates and overtakes as they happen.
          ALERT_WEBHOOK_URL: ${{ secrets.ALERT_WEBHOOK_URL }}
          WARM_BASE_URL: ${{ vars.WARM_BASE_URL }}
      - name: Commit snapshot
        run: |
          git config user.name "warpchart-bot"
          git config user.email "warpchart-bot@users.noreply.github.com"
          git add data/
          git diff --cached --quiet || {
            git commit -m "snapshot $(date -u +%FT%H:%MZ)"
            git pull --rebase
            git push
          }
